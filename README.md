# AI-speech-to-text-model
**AI SmartNotes** is a smart lecture assistant that records classroom audio using an ESP32 device and converts it into structured, summarized notes using speech-to-text and NLP. It generates clear study material with headings, bullet points, and key highlights via a simple web interface.
